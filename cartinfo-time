
/*carts providing customer based on registration and mentioning time*/
package com;
import prod.*;

import java.util.Date;

class CartsInfo implements Runnable {

 // Available carts are
 int available = 10;
 //int wanted;

// int ch;

/* public CartsInfo(int i) {
      wanted = i;
 }*/

 @SuppressWarnings("deprecation")
@Override
 public void run() {
      synchronized (this) {
          // Display available carts
          System.out.println("Available carts= " + available);
  
       //  if (available >= wanted) {
               String name = Thread.currentThread().getName();
               System.out.println( " Carts(s) reserved for " + name);
               available--; 
               
              // try {
            	   //Date d =new Date();
            	  // d.getTime();
            	 //  System.out.println(d);
                  // System.out.println("customer carttaking time:"+d.getDate());
                    /*try {
						Thread.sleep(3000);
					} catch (InterruptedException e) {
						// TODO Auto-generated catch block
						e.printStackTrace();
					}//waiting*/
                    //available = available - wanted;
                    // update the no. of available carts
              // } catch (InterruptedException ie) {
                  //  ie.printStackTrace();
               }
          } 
        //  else {
               // if available carts are less, display sorry
              // System.out.println("Sorry! carts already in use wait for some time");
        //  }
     // }

 }


